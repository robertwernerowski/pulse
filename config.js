// Pulse configuration
// --------------------
// The dashboard works fully as a static demo with no setup — it shows
// realistic sample AI output so the page never errors on GitHub Pages.
//
// To enable LIVE AI suggestions, deploy the serverless proxy in /api
// (see README → "Enabling live AI") and paste its URL below:
//
//   window.PULSE_PROXY_URL = "https://your-project.vercel.app/api/suggest";
//
// Leaving it empty keeps the safe, self-contained demo behaviour.

window.PULSE_PROXY_URL = "";
